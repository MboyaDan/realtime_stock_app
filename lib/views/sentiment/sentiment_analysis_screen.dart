import 'package:flutter/material.dart';

class SentimentAnalysisScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Sentiment Analysis Screen"),
      ),
    );
  }
}
